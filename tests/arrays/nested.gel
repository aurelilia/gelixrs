/*
*/

This test causes SIGSEGV.
This text prevents that with a parse error.

func main() {
    var arr = Array::<Array<i64>>(2)
    arr.push(Array::<i64>(2))
    arr.get(0).push(13)
    print(arr.get(0).get(0))
}
