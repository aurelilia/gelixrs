/*
5
*/

func main() {
    // Variadic can only be on external functions.
    // ToString on isize uses snprintf, which is variadic;
    // so this test will fail if snprintf fails to compile.
    print(5)
}