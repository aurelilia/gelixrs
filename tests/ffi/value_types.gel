/*
*/

import std/memory/+

func main() {
    val t = Test()
    TODO: This causes a stack overflow?
    hm(load_value(t))
    free::<Test>(t)
}

func hm(a: ^Test) {}

extern class Test {
    val a = 4
}